<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home page</title>

    <style>
        body {
            background-color: #1e1e1e;
            color: #eaeaea;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
            margin: 0;
            padding: 0;
        }

        .container {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 25px;
            text-align: center;
        }

        h2 {
            font-weight: 600;
            letter-spacing: 0.5px;
        }

        h2 span {
            color: #22c55e; /* green accent for count */
        }

        h2::after {
            content: "";
            display: block;
            width: 170px;
            height: 3px;
            background-color: #2563eb;
            margin: 12px auto 0;
            border-radius: 2px;
        }

        .btn {
            padding: 10px 20px;
            font-size: 15px;
            font-weight: 600;
            border-radius: 8px;
            cursor: pointer;
            border: none;
            transition: background-color 0.2s ease, transform 0.1s ease;
        }

        .btn-primary {
            background-color: #2563eb;
            color: #ffffff;
        }

        .btn-primary:hover {
            background-color: #1d4ed8;
        }

        .btn-success {
            background-color: #16a34a;
            color: #ffffff;
        }

        .btn-success:hover {
            background-color: #15803d;
        }

        .btn:active {
            transform: scale(0.97);
        }
    </style>
</head>

<body>
    <div class="container">
        <h2>
            Total number of users in our DB =
            <span><%= users %></span>
        </h2>

        <form method="get" action="/user">
            <button class="btn btn-primary">SHOW DATABASE</button>
        </form>
        <form method="get" action="/user/add">
            <button class="btn btn-success">Join now!</button>
        </form>
    </div>
</body>
</html>
